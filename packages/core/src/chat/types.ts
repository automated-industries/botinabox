/**
 * Chat types — re-exports from @botinabox/shared channel types.
 * Story 4.1
 */

export type {
  ChatType,
  FormattingMode,
  ChannelCapabilities,
  ChannelMeta,
  InboundMessage,
  Attachment,
  OutboundPayload,
  SendResult,
  HealthStatus,
  ChannelConfig,
  ChannelAdapter,
} from "@botinabox/shared";
